# Drop Blocks

> An AngularJS project using socket.io and Express - simple multiplayer game with customization features

## Build setup

``` bash
# install dependencies
npm install

# serve at localhost:3000
npm run 
```
